---
apiVersion: v1
kind: Service
metadata:
  name: inference-router
  namespace: devops-genai
spec:
  selector:
    app: inference-router
  ports:
    - name: http
      port: 8000
      targetPort: 8000
